<?php
/* 
 * We will configure the system via 2-dimensional arrays. It may be worthwhile
 * building a configuration class into the system at some point in the future
 */

$config['ldap']['server'] = "ldap://ldap.geeksoc.org";
$config['ldap']['dn'] = "ou=People,dc=geeksoc,dc=org";